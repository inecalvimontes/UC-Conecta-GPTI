# UC-Conecta-GPTI

Starter frontend setup for:
- Landing / Explore page (`index.html`)
- Login / Register page (`auth.html`)

## Run locally

Because this is a static frontend starter, you can open the files directly or run a simple local server:

```bash
npm install
npm run dev
```

Then open `http://localhost:XXXX`.
